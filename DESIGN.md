# PachiPakuGen UI Design

## 基本方針

PachiPakuGenは7STEPを繰り返し操作する制作ツールです。装飾より、工程の現在地、プレビュー、次に押す操作が一目で分かることを優先します。ライト／ダークの両モードで同じ意味とコントラストを保ちます。

## 色の役割

- ピンク: 生成、保存、出力、編集完了など結果を確定する主操作
- 青: 選択、編集、移動、戻る、プレビュー操作
- 緑: 完了済み状態。通常操作の強調には使わない
- 赤: エラーと破壊的な警告
- 黄: 注意と未確定の警告だけ
- 現在STEP: 高コントラストの青。完了STEPの緑と明確に分離

薄い青地と緑枠を同じ面積で重ねないでください。ライトモードでは完了色の彩度を抑え、現在地と主操作を最も強く見せます。

## 共通レイアウト

```text
7STEPタブ + 小型ユーティリティアイコン
本文（必要に応じて左操作／右プレビュー）
固定下部操作（戻る／状態／主操作）
```

- ヘッダー、タブ、本文、固定下部操作を同じ左右基準線へそろえる。
- STEP 1〜7の見出しは同じ上余白、ラベル、文字サイズを使う。
- STEP 4〜7は原則として左に操作、右に選択可能なプレビューを置く。
- STEP 3は処理設定中心で、常設プレビューを置かない。
- 下部の主操作は状態に応じて「実行」から「次へ」へ切り替える。
- ボタン文言は一行に収め、状態変化で隣の要素を動かさない。
- 唯一の「再編集」「開始」操作は、見つけやすい横長ボタンにする。

## モーション調整

- 左ペインはスクロール可能な設定、右側は拡大・縮小・ドラッグ可能なプレビュー。
- よく使う効果と方式／テンプレートを上部、回転軸など細部を詳細へ置く。
- 説明文を常時積み上げず、項目名のヘルプへ寄せる。
- キャプチャ表示は通常プレビューと同じ表示範囲・倍率から開始する。
- マイク／OBS操作は素材編集と混同しないが、STEP 7とスタートメニューから到達可能にする。

## 禁止事項

- 完了状態と現在状態を同じ色で表現しない。
- 画像処理やモデル取得を画面遷移だけで暗黙に開始しない。
- 情報量の少ないSTEPを無意味なカードで埋めない。
- ベース画像など選択対象でないものを表情選択リストへ表示しない。
- 保存済みと未保存、SpriTalk基本画像と未対応のv2モーション設定を同じ互換性として表示しない。
